{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "bash scripts/sync-content.sh",
    "dev": "bash scripts/sync-content.sh && astro dev",
    "build": "bash scripts/sync-content.sh && astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0",
    "astro": "^6.1.6",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
